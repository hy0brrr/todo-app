# Todo App Project Instructions

- After code changes, relaunch the standalone macOS Demo (`tidy Demo.app`) for inspection. Never kill or relaunch the installed production app (`tidy.app`).
- Use `TodoApp/TodoApp/Helpers/DesignTokens.swift` for UI tokens. Prefer tokens over hardcoded style values, and keep functional logic separate from token changes.
